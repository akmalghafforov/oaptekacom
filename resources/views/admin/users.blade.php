@extends('layouts.app')
@section('content')
<x-ui.page-header title="Пользователи" description="Управление доступом к платформе."><x-slot:actions><a class="text-sm font-semibold text-brand-700 hover:underline" href="{{ route('admin.index') }}">Операционный центр</a></x-slot:actions></x-ui.page-header>
<div class="table-wrap"><table class="data-table"><thead><tr><th>Пользователь</th><th>Организация</th><th>Роль</th><th>Тариф</th><th class="text-right">Действие</th></tr></thead><tbody>@foreach($users as $user)<tr><td class="font-medium">{{ $user->name }}</td><td>{{ $user->organization?->name ?? '—' }}</td><td>{{ $user->role->value }}</td><td>{{ $user->subscription_plan->label() }}</td><td class="text-right">@if(!$user->isAdmin())<div class="flex flex-wrap justify-end gap-2">@if($user->isCustomer())<a class="inline-flex min-h-10 items-center rounded-control border border-slate-300 px-4 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50" href="{{ route('admin.subscriptions.index', ['user_id' => $user->id]) }}">Подписка</a>@endif<form method="post" action="{{ route('admin.block', $user) }}">@csrf <x-ui.button :variant="$user->is_blocked ? 'secondary' : 'danger'">{{ $user->is_blocked ? 'Разблокировать' : 'Заблокировать' }}</x-ui.button></form>@if(($user->isCustomer() || $user->isWholesaler()) && $user->is_blocked)<form method="post" action="{{ route('admin.phone.remediate', $user) }}" class="flex gap-2">@csrf <x-ui.input name="phone" label="Телефон" required placeholder="+992901234567" /><x-ui.button variant="secondary">Исправить телефон</x-ui.button></form>@endif</div>@endif</td></tr>@endforeach</tbody></table></div>
<div class="mt-6">{{ $users->links() }}</div>
@endsection
