@extends('layouts.app') @section('content')<h1>Операционный центр</h1><h2>Ожидают одобрения</h2>@forelse($pending as $o)<div class="card">{{$o->name}} · {{$o->phone}}<form method="post" action="{{route('admin.approve',$o)}}">@csrf<button>Одобрить и включить демо</button></form></div>@empty<p>Нет заявок.</p>@endforelse<h2>Запросы на оплату</h2>@forelse($payments as $p)<div class="card">{{$p->organization->name}}: {{$p->days}} дн. / {{$p->amount}} TJS<form method="post" action="{{route('admin.payment',$p)}}">@csrf <input name="note" placeholder="Комментарий"><button name="action" value="approve">Подтвердить</button><button name="action" value="reject">Отклонить</button></form></div>@empty<p>Нет запросов.</p>@endforelse<h2>Подписки истекают</h2>@foreach($expiring as $o)<p>{{$o->name}} — {{$o->subscription_until}}</p>@endforeach@endsection
