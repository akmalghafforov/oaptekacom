@extends('layouts.app')
@section('content')
<x-ui.page-header title="Каталог лекарств" description="Сравнивайте актуальные предложения поставщиков. Цена указана в TJS." />
<x-ui.card class="mb-6"><form class="grid gap-3 md:grid-cols-[minmax(0,1fr)_12rem_auto] md:items-end"><x-ui.input name="q" label="Поиск" :value="$q" placeholder="Название, МНН или форма" hint="Поиск начинается с двух символов." /><x-ui.input name="city" label="Город" :value="request('city')" placeholder="Например, Душанбе" /><x-ui.button>Найти</x-ui.button></form></x-ui.card>
<div class="grid gap-4 md:grid-cols-2 xl:grid-cols-3">@forelse($offers as $offer)<x-ui.card class="flex flex-col gap-4"><div><p class="text-lg font-bold text-ink">{{ $offer->medicine->name }}</p><p class="mt-1 text-sm text-slate-600">{{ $offer->medicine->dosage }} · {{ $offer->medicine->form }}</p><p class="mt-3 text-sm text-muted">{{ $offer->organization->name }}, {{ $offer->organization->city }} · {{ $offer->medicine->manufacturer }}</p></div><div class="mt-auto flex items-end justify-between gap-3"><div><p class="text-2xl font-bold text-brand-700">{{ $offer->price }} <span class="text-sm font-medium">TJS</span></p>@if($offer->old_price)<del class="text-sm text-muted">{{ $offer->old_price }} TJS</del>@endif<p class="mt-1 text-sm text-slate-600">В наличии: {{ $offer->stock }} шт.</p></div>@auth<form method="post" action="{{ route('cart.add', $offer) }}">@csrf <x-ui.button>В корзину</x-ui.button></form>@endauth</div>@if($offer->expires_at?->lt(now()->addMonths(5)))<x-ui.alert type="warning">Срок годности менее 5 месяцев</x-ui.alert>@endif</x-ui.card>@empty<div class="md:col-span-2 xl:col-span-3"><x-ui.empty-state title="Предложений не найдено" description="Измените запрос или параметры поиска." /></div>@endforelse</div>
<div class="mt-6">{{ $offers->links() }}</div>
@endsection
