<!doctype html><html lang="ru" data-theme="{{ auth()->user()?->theme ?? 'light' }}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>OAPTEKA</title><style>body{font:16px system-ui;margin:0;background:#f5f7fb;color:#172033}nav{background:#fff;padding:1rem;display:flex;gap:1rem;align-items:center;flex-wrap:wrap}main{max-width:1100px;margin:auto;padding:1.25rem}a{color:#1456b8;text-decoration:none}button,input,select{padding:.55rem;border:1px solid #ccd3df;border-radius:.35rem}button{background:#1456b8;color:#fff;cursor:pointer}.card{background:#fff;padding:1rem;margin:.75rem 0;border-radius:.5rem;box-shadow:0 1px 3px #0001}.muted{color:#667085}.alert{padding:.75rem;background:#e7f6ea}.danger{color:#b42318}table{width:100%;border-collapse:collapse}td,th{padding:.6rem;border-bottom:1px solid #ddd;text-align:left}@media(max-width:600px){nav{gap:.55rem;font-size:.9rem}main{padding:.75rem}table{font-size:.84rem}}</style></head><body><nav><strong>OAPTEKA</strong><a href="{{ route('catalog') }}">Каталог</a>@auth <a href="{{ route('dashboard') }}">Кабинет</a><a href="{{ route('cart') }}">Корзина</a><a href="{{ route('orders.index') }}">Заказы</a>@if(auth()->user()->isAdmin())<a href="{{ route('admin.index') }}">Администрирование</a>@endif<form method="post" action="{{ route('logout') }}">@csrf<button>Выйти</button></form>@endauth @guest <a href="{{ route('login') }}">Войти</a><a href="{{ route('register') }}">Регистрация</a>@endguest</nav><main>@if(session('success'))<p class="alert">{{session('success')}}</p>@endif @if(session('warning'))<p class="danger">{{session('warning')}}</p>@endif @if($errors->any())<p class="danger">{{ $errors->first() }}</p>@endif @yield('content')</main></body></html>
