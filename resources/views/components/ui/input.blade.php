@props(['name', 'id' => null, 'label' => null, 'hint' => null, 'type' => 'text', 'value' => null, 'maskPlaceholder' => null])
@php($controlId = $id ?? $name)
@php($descriptionIds = collect([$hint ? $controlId.'-hint' : null, $errors->has($name) ? $controlId.'-error' : null])->filter()->implode(' '))
<div class="space-y-1.5"><label for="{{ $controlId }}" class="block text-sm font-medium text-slate-700">{{ $label ?? $slot }}</label><div @class(['relative' => $maskPlaceholder])>@if($maskPlaceholder)<span data-phone-mask-placeholder aria-hidden="true" class="pointer-events-none absolute inset-y-0 left-3 flex items-center text-slate-900">{{ $maskPlaceholder }}</span>@endif<input id="{{ $controlId }}" name="{{ $name }}" type="{{ $type }}" value="{{ old($name, $value) }}" @if($descriptionIds) aria-describedby="{{ $descriptionIds }}" @endif @if($errors->has($name)) aria-invalid="true" @endif {{ $attributes->class(['block w-full rounded-control border px-3 py-2.5 shadow-xs placeholder:text-slate-400', 'relative z-10 bg-transparent text-transparent caret-slate-900' => $maskPlaceholder, 'bg-white text-slate-900' => ! $maskPlaceholder, 'border-danger' => $errors->has($name), 'border-slate-300' => ! $errors->has($name)]) }}></div>@if($hint)<p id="{{ $controlId }}-hint" class="text-xs text-muted">{{ $hint }}</p>@endif @error($name)<p id="{{ $controlId }}-error" class="text-xs text-danger">{{ $message }}</p>@enderror</div>
