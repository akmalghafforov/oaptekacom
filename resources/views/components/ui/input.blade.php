@props(['name', 'label' => null, 'hint' => null, 'type' => 'text', 'value' => null, 'maskPlaceholder' => null])
<div class="space-y-1.5"><label for="{{ $name }}" class="block text-sm font-medium text-slate-700">{{ $label ?? $slot }}</label><div @class(['relative' => $maskPlaceholder])>@if($maskPlaceholder)<span data-phone-mask-placeholder aria-hidden="true" class="pointer-events-none absolute inset-y-0 left-3 flex items-center text-slate-900">{{ $maskPlaceholder }}</span>@endif<input id="{{ $name }}" name="{{ $name }}" type="{{ $type }}" value="{{ old($name, $value) }}" {{ $attributes->class(['block w-full rounded-control border px-3 py-2.5 shadow-xs placeholder:text-slate-400', 'relative z-10 bg-transparent text-transparent caret-slate-900' => $maskPlaceholder, 'bg-white text-slate-900' => ! $maskPlaceholder, 'border-danger' => $errors->has($name), 'border-slate-300' => ! $errors->has($name)]) }}></div>@if($hint)<p class="text-xs text-muted">{{ $hint }}</p>@endif @error($name)<p class="text-xs text-danger">{{ $message }}</p>@enderror</div>
