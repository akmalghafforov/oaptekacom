@props(['name', 'label' => null, 'hint' => null, 'type' => 'text', 'value' => null])
<div class="space-y-1.5"><label for="{{ $name }}" class="block text-sm font-medium text-slate-700">{{ $label ?? $slot }}</label><input id="{{ $name }}" name="{{ $name }}" type="{{ $type }}" value="{{ old($name, $value) }}" {{ $attributes->class(['block w-full rounded-control border bg-white px-3 py-2.5 text-slate-900 shadow-xs placeholder:text-slate-400', 'border-danger' => $errors->has($name), 'border-slate-300' => ! $errors->has($name)]) }}>@if($hint)<p class="text-xs text-muted">{{ $hint }}</p>@endif @error($name)<p class="text-xs text-danger">{{ $message }}</p>@enderror</div>
