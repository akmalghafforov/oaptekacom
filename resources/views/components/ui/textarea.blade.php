@props(['name', 'label', 'hint' => null, 'value' => null])
<div class="space-y-1.5"><label for="{{ $name }}" class="block text-sm font-medium text-slate-700">{{ $label }}</label><textarea id="{{ $name }}" name="{{ $name }}" {{ $attributes->class(['block min-h-24 w-full rounded-control border bg-white px-3 py-2.5 text-slate-900', 'border-danger' => $errors->has($name), 'border-slate-300' => ! $errors->has($name)]) }}>{{ old($name, $value) }}</textarea>@if($hint)<p class="text-xs text-muted">{{ $hint }}</p>@endif @error($name)<p class="text-xs text-danger">{{ $message }}</p>@enderror</div>
