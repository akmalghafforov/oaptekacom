@extends('layouts.app') @section('content')<h1>{{ $organization->name }}</h1><p>Подписка: {{ $organization->subscription_until?->format('d.m.Y H:i') ?? 'не активна' }} · <a href="{{route('subscription.create')}}">Продлить</a></p><h2>Последние заказы</h2>@forelse($orders as $order)<p class="card"><a href="{{route('orders.show',$order)}}">Заказ №{{ $order->id }}</a> — {{ $order->status }} · {{ $order->total }} TJS</p>@empty<p>Заказов пока нет.</p>@endforelse @endsection
