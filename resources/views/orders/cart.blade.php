@extends('layouts.app') @section('content')<h1>Корзина</h1>@forelse($cart->items as $item)<div class="card"><strong>{{$item->offer->medicine->name}}</strong> · {{$item->offer->organization->name}}<br>{{$item->unit_price}} TJS <form method="post" action="{{route('cart.update',$item)}}">@csrf @method('PATCH')<input type="number" name="quantity" min="1" value="{{$item->quantity}}"><button>Изменить</button></form></div>@empty<p>Корзина пуста.</p>@endforelse @if($cart->items->isNotEmpty())<form method="post" action="{{route('cart.checkout')}}">@csrf<button>Оформить — заказы будут разделены по поставщикам</button></form>@endif @endsection
